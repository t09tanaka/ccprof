{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/t09tanaka/ccprof/main/schemas/organization-policy.schema.json",
  "title": "ccprof signed organization policy",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "policy_schema_version",
    "organization",
    "minimum_privacy",
    "allow_raw",
    "allow_advisory",
    "allow_export",
    "raw_retention_days_max",
    "required_source_coverage"
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "pattern": "\\S"
    },
    "policy_schema_version": {
      "const": 1
    },
    "organization": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$"
    },
    "minimum_privacy": {
      "enum": ["strict", "balanced", "raw"]
    },
    "allow_raw": {
      "type": "boolean"
    },
    "allow_advisory": {
      "type": "boolean"
    },
    "allow_export": {
      "type": "boolean"
    },
    "raw_retention_days_max": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991
    },
    "required_source_coverage": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "approval_policy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["safe_patterns", "allow_rule_recommendation"],
      "x-ccprof-runtime-constraints": [
        "UTF-8 byte limits are enforced by runtime",
        "NFC and whitespace normalization are enforced by runtime",
        "normalized duplicate rejection is enforced by runtime",
        "data descriptor and proxy rejection is enforced by runtime",
        "canonical signed payload byte limit is enforced by runtime",
        "full tuple ordering uses UTF-8 bytes at runtime",
        "monotonic merge rules are enforced by runtime"
      ],
      "properties": {
        "safe_patterns": {
          "type": "array",
          "maxItems": 64,
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 256
          }
        },
        "allow_rule_recommendation": { "type": "boolean" }
      }
    },
    "resource_domains": {
      "type": "array",
      "maxItems": 64,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["match", "domain", "parallel_safe"],
        "x-ccprof-runtime-constraints": [
          "UTF-8 byte limits are enforced by runtime",
          "NFC and whitespace normalization are enforced by runtime",
          "normalized duplicate rejection is enforced by runtime",
          "data descriptor and proxy rejection is enforced by runtime",
          "canonical signed payload byte limit is enforced by runtime",
          "full tuple ordering uses UTF-8 bytes at runtime",
          "monotonic merge rules are enforced by runtime"
        ],
        "properties": {
          "match": {
            "type": "array",
            "minItems": 1,
            "maxItems": 32,
            "items": {
              "type": "string",
              "minLength": 1,
              "maxLength": 256
            }
          },
          "domain": {
            "type": "string",
            "maxLength": 64,
            "pattern": "^[a-z0-9][a-z0-9._-]{0,63}$"
          },
          "parallel_safe": { "type": "boolean" }
        }
      }
    },
    "kill_switches": {
      "type": "object",
      "additionalProperties": false,
      "required": ["raw", "advisory", "export"],
      "properties": {
        "raw": { "type": "boolean" },
        "advisory": { "type": "boolean" },
        "export": { "type": "boolean" }
      }
    }
  }
}
